<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yangshuai.tiktok">

    <application
        android:name="com.yangshuai.tiktok.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/app_network_security_config"
        android:roundIcon="@mipmap/app_icon_jzsh"
        android:supportsRtl="true"
        android:theme="@style/UJUZAppTheme"
        tools:replace="android:theme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.yangshuai.tiktok.SelectIdentityActivity"
            android:exported="true" />
        <activity android:name="com.yangshuai.tiktok.SplashActivity"
            android:theme="@style/UJUZActivityTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 屏幕适配方案 -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="375" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="667" />
    </application>

</manifest>